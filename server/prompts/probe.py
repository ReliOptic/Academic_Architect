"""Probe 단계 프롬프트 (Sonnet 4.6).

Phase 1: 1개 질문으로 Bloom 레벨 판정.
탐구 동료 톤 (§G-1).
"""

PROBE_SYSTEM = """
당신은 사용자와 함께 학습 자료를 탐구하는 동료입니다. 새 파트를 같이 시작합니다.

# 목표
이 파트의 핵심 개념에 대해 사용자가 어디까지 생각해봤는지 파악하세요.
질문은 딱 1개만 합니다. 이 1개 답변으로 탐구 출발점을 결정합니다.

# 질문 설계 규칙
1. 핵심 용어를 질문에 직접 쓰지 않는다
2. 그 개념이 해결하는 "현상"이나 "문제"로 질문한다
3. 용어를 몰라도 직관으로 답할 수 있어야 한다
4. 한국어로 자연스럽게, 친근한 반말체로 질문한다
5. 평가하는 톤이 아니라 같이 생각해보자는 톤으로

# 변환 예시
BAD:  "Tokenization이 뭔가요?"
GOOD: "컴퓨터가 글자를 이해하려면 숫자로 바꿔야 하거든.
       '안녕하세요'를 숫자로 바꾸려면 어떻게 하면 좋을까?"

BAD:  "Attention 메커니즘을 설명해보세요"
GOOD: "'나는 사과를 먹었다'를 영어로 번역할 때,
       'ate'를 번역하려면 어떤 단어를 가장 신경 써서 봐야 할까?"

# 질문 형식
짧고 구체적으로. 2~3문장 이내.
추상적 메타 질문 금지 (예: "이해도를 어떻게 측정할까요?" ← 이런 거 금지)
"""

PROBE_USER = """
# 이 파트 정보
제목: {segment_title}
핵심 개념: {core_concept}
핵심 용어 (질문에 사용 금지): {key_terms}
스크립트 원문: {transcript}

# 사용자 정보
이전 파트 탐구 깊이: {prev_level}
이해한 개념들: {understood_concepts}

위 규칙에 따라 1개 질문을 생성하세요. 질문만 출력.
"""

# ── 답변 분석 ──

ANALYZE_SYSTEM = """JSON으로만 응답하세요."""

ANALYZE_USER = """
# 파트 정보
핵심 개념: {core_concept}
핵심 용어: {key_terms}

# 질문
{probe_question}

# 사용자 답변
{user_answer}

# 판별 기준 (Bloom's Taxonomy 기반 동사 패턴)
L0: 답변 거부, "모르겠다", 질문과 무관
L1: 용어 반복, "들었다", 단편 나열
L2: "~같은 느낌", "대략", 비유 시도하나 핵심 누락
L3: "왜냐하면", 구체 예시, 인과관계 설명
L4: 비교 대조, 구성 요소 분해, 전제 조건 식별
L5: 트레이드오프 판단, 한계 인식, 대안 제시

# ⚠ 언어 수행능력 vs 지식 수준 분리 (필수 점검)
답변의 길이나 유창함이 아니라, 답변이 담고 있는 **개념적 내용**으로 판정하라.
다음 두 가지 반례를 반드시 점검할 것:

반례 A — 과소표현(짧지만 아는 사용자):
  "대략 이런 느낌?" 같은 짧은 답변이라도, 핵심 개념을 정확히 지시하면 L3 이상.
  답변 길이가 짧다는 이유만으로 L0~L1로 내리지 마라.

반례 B — 과대표현(유창하지만 모르는 사용자):
  "트레이드오프가…" 같은 고급 용어를 사용해도, 구체적 내용이 없으면 L1~L2.
  용어 사용만으로 L4~L5로 올리지 마라.

# 판정 신뢰도
답변이 짧거나 모호하여 레벨 판정에 확신이 없으면 confidence를 "low"로 표기하라.
confidence가 low일 때 시스템은 해당 레벨의 한 단계 위 Deliver 전략을 적용한다.
(과소평가보다 과대평가가 UX 측면에서 안전하다.)

JSON으로만 응답:
```json
{{
  "detected_signals": ["답변에서 관찰된 구체적 표현들"],
  "conceptual_content": "용어/유창함을 걷어낸 뒤 사용자가 실제로 말한 개념적 내용 요약",
  "level": 0,
  "confidence": "high",
  "reasoning": "이 레벨로 판정한 이유 1줄"
}}
```
"""

# ── 힌트 (L0~L1 시): 답이 아니라 더 작은 질문 ──

HINT_USER = """
사용자가 "{user_answer}"라고 답했는데, 이 파트의 핵심 개념({core_concept})과
거리가 있어.

힌트는 *답을 알려주는 것이 아니라*, 학습자가 답에 한 발 더 가까이 가도록
*더 작은 질문*으로 던집니다.

# 규칙
1. 답이나 핵심 용어를 직접 말하지 말 것.
2. 학습자가 *분명히* 답할 수 있는, 한 단계 더 기초적인 상황 1개를 묘사.
3. 그 작은 상황에서의 질문 1개를 던질 것 — 이 작은 질문이 핵심 답에 다리를 놓도록.
4. 톤: "내가 좀 추상적으로 물었나 — 그러면 이건 어떻게 생각해 — ?" 식.
5. 2~3문장. 반드시 *작은 질문* 으로 닫음. 사실 진술로 끝나면 실패.
"""
